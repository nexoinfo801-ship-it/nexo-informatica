name: NEXO Gateway CI

on:
  push:
    branches: [nexo-gateway-prep, nexo-commercial-hardening-r3]
    paths:
      - "gateway/**"
      - ".github/workflows/gateway-ci.yml"
  pull_request:
    paths:
      - "gateway/**"
      - ".github/workflows/gateway-ci.yml"

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: gateway
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: gateway/package.json
      - name: Install
        run: npm install --ignore-scripts --no-audit --no-fund
      - name: Syntax check
        run: node --check server.mjs && node --check hub.mjs && node --check hub_store.mjs
      - name: Test gateway + mobile integration
        run: npm test
